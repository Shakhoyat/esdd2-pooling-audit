# esdd2-pooling-audit

[![Code licence: MIT](https://img.shields.io/badge/code%20licence-MIT-blue.svg)](LICENSE)
[![Data licence: CC BY-NC 4.0](https://img.shields.io/badge/data%20licence-CC%20BY--NC%204.0-lightgrey.svg)](data/LICENSE-DATA.md)
[![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue.svg)](requirements.txt)
[![ICASSP 2027: under review](https://img.shields.io/badge/ICASSP%202027-under%20review-orange.svg)](#citation)

Code, per-clip scores and scripts that reproduce every number, table and figure in
**"Metric Identifiability Under Structural-Zero Pooling in Audio Anti-Spoofing"**
(submitted to ICASSP 2027, under review).

The paper itself is not included while it is under review.

## What the paper claims

Component-level anti-spoofing scores a recording's speech and its environment on separate axes. An
untouched capture has no separately generated component, so on both axes it is a *structural zero*,
not a missing value, yet the ESDD2 protocol pools such clips with bona fide mixtures. The paper
shows that this stated rule distorts the equal error rate it reports. Pooling structural zeros mixes
ROC curves: areas survive, and the EER admits no aggregation rule (Theorem 1 and Proposition 1). A published
pooled EER therefore leaves the component-only EER in an interval (Theorem 2), and on ESDD2 that interval
fails to determine any pairwise comparison on either axis. No label-free per-clip score reduction repairs this;
the correction belongs in the evaluation protocol. The organiser already holds the partition, so we propose
ACDS (Definitions 1-3), which recovers the component-only value exactly when the partition is available and
prices alarms on inapplicable inputs by an explicit cost rather than a split's class proportions
(Propositions 2 and 3).

## Claim → script → test

Every numeric literal is listed in `paper_numbers.yaml` with its location, printed precision and the
value that reproduces it. The values there are **transcribed by hand from the LaTeX source**, not
generated by this code, so the check is not circular.

| where in the paper | claim | script | test |
|---|---|---|---|
| Fig. 2, the ACDS mechanism | the three areas 0.8740, 0.7130 and 0.1611, read at one operating point | `scripts/fig_acds_mechanism.py` | `test_claim[Fig_2_*]` |
| §4.1 | evaluation clips, sample rate, trapezoid-vs-step gap | `src/pooling_audit/acds_artefacts.py` | `test_claim[sec4_1_*]` |
| Table 1, §4.2, §4.4 | all twenty cells; the two Proposition 2 identities to 1e-15; the five crossings, the last at 0.125; w₀/wₐ = 1.138; the spans 0.053 and 0.706 | `scripts/acds_table.py` | `test_claim[Table_1_*]`, `test_claim[sec4_4_*]`, `test_table_rows_are_verbatim_in_the_paper[table1.tex]` |
| §4.2 | the fill-constant spread 0.3970, the BEATs Linear endpoints, the EAT Linear c = 0 cell, the ASVspoof 5 minDCF residual | `scripts/table1.py` | `test_claim[sec4_2_*]` |
| §4.3 | the pooling/head decomposition | `scripts/s4_ablation.py` | `test_claim[sec4_3_*]` |
| §4.5 | the gate's λ and ρ on both axes, the gated and annotated EERs, and the four errors | `scripts/acds_gate.py` | `test_claim[sec4_5_*]` |
| Fig. 1, Theorem 2's identified sets | the released code constructs an admissible system matching the ESDD2 baseline's published pooled EER, at the lower endpoint of the identified interval | `scripts/fig_identified.py` | `test_claim[Fig_1_*]` |
| Fig. 3, the α₀ sweep | each system's line is AUCₐ − α₀(1 − AUC₀); the last crossing at 0.125 | `scripts/fig_accurve.py` | `test_claim[sec4_4_*]` |

`verify.py` recomputes all of them from per-clip scores. `tests/test_claims.py` checks the same
entries against the committed `results/`, so it runs without any data; `results/MANIFEST.json`
records the inputs those results were built from.

## Sixty-second quickstart

On a clean checkout, with nothing downloaded:

```bash
pip install -r requirements.txt
make test          # the paper's numbers against committed results, plus unit tests
```

To recompute from per-clip scores rather than trust the committed results:

```bash
python tools/fetch_data.py --labels-only      # 2.4 MB; accept the CompSpoofV2 licence first
make verify                                   # about three minutes on CPU
```

`make all` regenerates `results/`: every number, Table 1 as a `.tex` row set, and all three figures.
`make gate` checks a local copy of the paper at `paper/main.tex` (not included; see "Paper gate").

## Data acquisition

**Tier 0 needs no audio.** CompSpoofV2 publishes `eval_label.csv` as a standalone file, and this
repository ships the two small derived files that would otherwise need the large archives.

| what | size | where from | unlocks |
|---|---|---|---|
| `eval_label.csv` | 2.4 MB | CompSpoofV2 on Hugging Face | every EER, Table 1's point estimates, §2, §3, §4.2, §4.3, §4.4, Fig. 1 |
| `data/derived/clusters.csv` | 1.4 MB | **shipped** | the clustered bootstrap intervals |
| `data/derived/native_format.csv` | 1.3 MB | **shipped** | format-related internal checks |
| `baseline_scores.txt` | — | ESDD2 baseline repository | the reference system |
| `development.tar.gz` | 88 GB | CompSpoofV2 | only the 175,361 and 24,864 clip counts |

With `eval_label.csv` and the shipped files, every entry verifies except those two counts: their
CSVs are 46 MB inside an 88 GB split archive that cannot be partially extracted. `verify.py` reports
any entry whose input is absent as not checked, with the reason, rather than passing it.

`clusters.csv` holds **anonymous cluster integers** — no source file, corpus or generator names —
and `native_format.csv` a sample rate and channel count per clip. Regenerate both from the archives
with `tools/derive_metadata.py` and compare against the sha256 in `MANIFEST.sha256`.
See `data/README.md` for the full terms.

## Environment

Developed on CPython 3.10.12; the pinned `requirements.txt` also installs cleanly on 3.13.
`check_paper_sync.py`, `tools/sync_paper.py` and the page gate need `pdftotext` and `pdfinfo`
(poppler-utils), and compiling the paper needs `pdflatex`. Tier 1 additionally needs `torch` and a
GPU; see `scripts/train/README.md`.

| tier | command | hardware | time |
|---|---|---|---|
| 0 | `make test` | CPU | about 1 s |
| 0 | `make verify` / `make all` | CPU, ~2 GB RAM | about 3 minutes, dominated by the bootstrap |
| 1 | `make regenerate` | one GPU (ours: RTX A6000) | feature extraction runs at ~191 clips/s; probe training is smoke-tested only |

## What this repository does not contain

- **No audio and no corpus files.** CompSpoofV2 is CC BY-NC 4.0 and access-gated; fetch it yourself.
- **No ESDD2 baseline code.** It is CC BY-NC 4.0; `src/pooling_audit/eer.py` is a reimplementation
  from its published description, citing the original file. The one behaviour it must preserve is
  that the reported EER is the *mean* of FAR and FRR at the nearest ROC vertex, which is what makes
  the reported value a step function of the fill constant.
- **No baseline `prediction.txt`.** It ships with the baseline repository.
- **No encoder weights.** `models/fetch_encoders.py` downloads and hash-verifies them.
- **No probe checkpoints.** They were never retained; see Roadmap.
- **No `spconf.sty`.** The ICASSP style file carries no licence statement; get it from the author kit.
- **No paper source or PDF.** The paper is under review. The tools that check it
  (`check_paper_sync.py`, `make gate`, `tests/test_paper_hygiene.py`) skip or ask for a path when it is absent.

---

## Licences

| what | licence |
|---|---|
| code: `src/`, `scripts/`, `tests/`, `tools/`, `models/`, `verify.py`, `check_paper_sync.py` | **MIT**, `LICENSE` |
| our per-clip scores, `data/derived/`, `results/` and the figures | **CC BY-NC 4.0**, `data/LICENSE-DATA.md`, with CompSpoofV2 attribution required |
| CompSpoofV2, the baseline scores | not redistributed |

## A numerical check is not a proof

Theorem 1 (collapsibility), Theorem 2 (sharp partial identification), Proposition 1 (no aggregation rule for the
EER, minDCF or Youden's index) and Propositions 2-3 (properties of ACDS, predicted applicability) are proved in
the paper. This repository does something weaker, and it matters which is which.

- `tests/test_identities.py` shows the pooling identities hold to machine precision on synthetic
  data: a check that the premise is implemented correctly, not a proof of it.
- `test_no_aggregation_rule_exists_for_the_crossing` builds two component pairs with identical
  component crossings and different pooled crossings. That is a genuine counterexample, and it does
  establish that no rule φ(T₀, Tₐ, w₀) exists for the crossing.
- `tests/test_bounds.py` checks the interval formula and the ordering criterion as arithmetic.
- `tests/test_prop2_witness.py` checks the counterexample construction twice. In exact rational arithmetic,
  which for a piecewise-linear ROC is a proof that the witness has the stated values. Then under the
  released scorer, on score arrays whose empirical ROC is each curve exactly, materialised both with
  one tied score per linear segment and with every vertex kept: every printed crossing and optimum is
  a genuine vertex, so the scorer's vertex rule reproduces the fractions in both. minDCF at ASVspoof 5's
  costs is β·P_miss + P_fa with β = (C_miss/C_fa)(1−π_spf)/π_spf = 1.9 (C_miss = 1, C_fa = 10,
  π_spf = 0.05), the Track 1 definition.
- `tests/test_sharpness.py` checks Theorem 2 in both directions, numerically: an explicit
  construction attains every sampled point of six intervals, endpoints included, to 10⁻¹², and no
  randomly drawn admissible system lands outside the interval. That is evidence for sharpness at the
  sampled points, not a proof that every point is attained; the proof is the paper's. Under the
  stronger conditioning on a submission's other published cells, only a lower-endpoint witness is
  built (`sharpness.lower_endpoint_witness`), and none is claimed for the upper endpoint.

## Paper gate

`make gate` runs `tests/test_paper_hygiene.py` over `paper/main.tex`: no surviving `TODO`, no
"development" (an earlier draft mislabelled the evaluation split), every citation resolving both
ways, and a compiled PDF of at most 5 pages with references alone on page 5. The page checks need a
compiled PDF: set `ESDD2_PAPER_PDF`, or place `spconf.sty` in `paper/`.

It is kept out of `make test` because it is a release condition rather than a correctness test.

## Provenance of the ported code

The trainers are **ports, not rewrites**: structure and every hyper-parameter come from the scripts
that produced the paper's scores. Absolute paths are gone, and all settings are read from
`configs/systems.yaml`, which `verify.py` also checks the paper's sampling-rate reference against.

| here | ported from | at commit |
|---|---|---|
| `src/pooling_audit/heads.py::ComponentHeads` | `heads.py` | `3346700` |
| `src/pooling_audit/heads.py::train_component_heads`, `score_component_heads` | `r2_04b_two_head.py` | `ec8ef38` |
| `src/pooling_audit/heads.py::train_linear_probe` | `probe_layers.py` | `d436aa8` |
| `scripts/train/extract_features.py` | `features.py`, `enc_beats.py` | `d436aa8` |
| `scripts/fig_identified.py`, `scripts/fig_acds_mechanism.py`, `scripts/fig_accurve.py`, `scripts/figstyle_pgf.py` | `figures/fig_identified.py`, `figures/fig_acds_mechanism.py`, `figures/fig_accurve.py`, `figures/figstyle_pgf.py` | identical text layers; pgf writes a fresh object id per run, so the bytes differ |
| `src/pooling_audit/sharpness.py::build_system`, `attainment_grid`, `containment` | `verify_theory.py` (C1, C1b) | `1fc3508` |
| `src/pooling_audit/sharpness.py::lower_endpoint_witness` | `audit_sharpness_witness.py`, **redesigned** | — |
| `src/pooling_audit/witness.py` | `witness_check.py`, `prop2_witness.py`, `mindcf_asvspoof5_witness.py` | — |

The lower-endpoint witness is not a straight port. The original placed the structural zeros below
every negative, which under the released scorer cannot reach the published 0.4336 (it tops out near
0.27), so it never actually held the published cell. This version spreads them among the negatives
and solves for the one free parameter, and it reproduces 0.4336 at four decimals.

**The committed figures in `results/` are the submitted PDFs**, not regenerated by
`scripts/fig_*.py` here. `scripts/fig_accurve.py` and `scripts/fig_identified.py` use the same system
names the submitted figures do, and both run to a valid PDF, but neither yet reproduces the small
zoomed inset that the submitted `fig_accurve.pdf` adds around its last pairwise crossing. `make figures`
writes to `build/`; `make all` overwrites `results/` with the simpler, inset-free versions, so restore
them with `git checkout results/` if you want the submitted figures back.

The commits listed above are in the authors' development history, which is not public; they are
recorded for traceability.

## Supplementary: the consistency experiment

`scripts/consistency_experiment/` holds an experiment that is **not reported in the paper**: it
retrains the component heads with and without Class 0 masked from the losses, backing the remark in
§4.3 that masked heads emit unspecified values on structural zeros. It was pre-registered before the
runs (commit `8694d0c` of the authors' development history). The per-clip scores of both arms ship in
`scores/consistency/`, so its four numbers verify in tier 0.

## Checkpoints were not retained

The probes were trained and their per-clip scores saved; the weights were not. Tier 1 regenerates
the probes from frozen encoder features rather than restoring the originals, and because training is
seeded and the recipe pinned, a rerun should land in the same place without being bit-identical.
**A full retraining has not been re-run since the training-split feature cache was lost**, so tier 1
is smoke-tested, not reproduced; `scripts/train/README.md` lists exactly which entry points that
covers.

## Roadmap

- **Full tier-1 retrain, after review.** Rebuild the training-split features from the corpus
  audio and rerun all three seeds.
- **Probe checkpoints, after that retrain,** published alongside the scores under CC BY-NC 4.0.
- **Port the `fig_accurve.pdf` inset**, so `make figures` reproduces the submitted figure exactly
  rather than a simpler, inset-free version (see "Provenance of the ported code" above).

Neither affects what the paper claims: every published number is verified in tier 0 against the
per-clip scores that produced it.

## Layout

```
verify.py              recompute every number from per-clip scores, compare, exit non-zero on failure
check_paper_sync.py    paper_numbers.yaml against a compiled PDF, in both directions
paper_numbers.yaml     every literal: value, precision, location, producing key
results/               artefacts.json, table1.tex, the three figure PDFs, MANIFEST.json
src/pooling_audit/     eer, pooling, bounds, sharpness, witness, regimes, sweep, bootstrap, consistency, heads, claims
scripts/               one per artefact, make_results.py, and tier 1 under train/ and consistency_experiment/
verify/                the printed-number manifest, its recomputation and VERIFICATION_REPORT.md
tests/                 test_claims, test_identities, test_bounds, test_sharpness, test_prop2_witness, test_scorer_equivalence, test_paper_hygiene
data/                  published/ (the eleven-system table) and derived/ (clusters, native format)
scores/                our per-clip scores, all systems and seeds, and both consistency arms
configs/               layer sets, seeds and training recipes
models/                encoder sources, sha256 and a verifying download script
tools/                 fetch_data, derive_metadata, sync_paper, manifest
```

## Citation

If you use this code or the released scores, please cite the paper (metadata in `CITATION.cff`):

```bibtex
@misc{shujon2027identifiability,
  title  = {Metric Identifiability Under Structural-Zero Pooling in Audio Anti-Spoofing},
  author = {Rahman Shujon, Md. Shakhoyat and Mahi, Khadimul Islam and Islam, Md. Milon and
            Haque, Md Rezwanul and Altaheri, Hamdi and Karray, Fakhri},
  note   = {Submitted to IEEE ICASSP 2027},
  year   = {2026}
}
```

This entry will be updated if the paper is accepted.

## Contact

Questions and issues: please open a GitHub issue, or email Md. Shakhoyat Rahman Shujon
(skt104.shujon@gmail.com).
